id: randomUser
displayName: Random User
path: /api/
method: GET
archetype: default
variables:
  seed:
    type: text
    default: bentobento
    displayName: Seed string (for stable results)
parameters:
  seed: ${seed}
dataSourceId: randomUsers
allowedOnComponents: []
